/**
 * @file main.cpp
 * @author  Royneal Rayess,
 * @date 13 Oct 2018
 * @copyright 2018  Royneal Rayess,
 * @brief This file implements the main A* algorithm
 */


#include <iostream>
#include <lib.hpp>

int main()
{
  // dummy();
  /* OpenList list;
  vector<Node> Open_list;
  vector<Node>::size_type i;
  vector<Node>& O_list = Open_list;

  Node node1, node2, node3, node4;
  node1.SetId(1);
  node1.SetF(1);
  node1.SetH(1);
  node2.SetId(2);
  node2.SetF(2);
  node2.SetH(2);
  node3.SetId(3);
  node3.SetF(3);
  node3.SetH(3);
  node4.SetId(4);
  node4.SetF(4);
  node4.SetH(4);

  list.Add(O_list, node4);
  list.Add(O_list, node3);
  list.Add(O_list, node2);
  list.Add(O_list, node1);

  std::cout << "size: " << O_list.size() << std::endl;
  for (i = 0; i < O_list.size(); i++) {
    std::cout << "i: " << i << std::endl;
    std::cout << O_list.at(i).GetId() << std::endl;
  }
  std::cout << "lowest id " << list.IsLowestF(Open_list) << std::endl;
   */
    return 0;
}
